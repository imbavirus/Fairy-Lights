# GitHub Configuration
# Required for GitHub releases
GITHUB_TOKEN=your_github_token_here

# CurseForge Configuration
# Required for CurseForge uploads (both must be set)
CF_PROJECT_ID=your_curseforge_project_id_here
CF_API_TOKEN=your_curseforge_api_token_here
# Note: CF_API_KEY, CURSEFORGE_API_TOKEN, and CURSEFORGE_API_KEY are also accepted (all refer to the same credential)

# Optional CurseForge Settings
# Comma-separated numeric game version IDs (e.g., 9990,68441)
# If not set, the script will attempt to auto-resolve from minecraft_version in gradle.properties
# CF_GAME_VERSION_IDS=9990,68441

# Release type: release | beta | alpha (default: release)
# CF_RELEASE_TYPE=release

# Optional path to a changelog file (markdown or text)
# If not set, automatically looks for changelogs/{version}.md (e.g., changelogs/3.0.1.md)
# CF_CHANGELOG_FILE=CHANGELOG.md

# Optional base URL for CurseForge API (default: https://minecraft.curseforge.com)
# CF_BASE_URL=https://minecraft.curseforge.com

# Optional mod loader name (default: NeoForge)
# CF_MOD_LOADER=NeoForge

# Server/Client environment IDs (e.g., 1,2 for Server and Client)
# If omitted, auto-resolves Server (1) and Client (2)
# CF_ENVIRONMENT_IDS=1,2

# Java version ID (e.g., Java 21)
# If omitted, auto-resolves Java 21
# CF_JAVA_VERSION_ID=12345
