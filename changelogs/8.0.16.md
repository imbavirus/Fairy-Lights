# Fairy Lights 8.0.16 - Pennant attach + save hang

## Pennant Bunting

- Flags hung ~1 block below the rope (CF report, reproduced `meka_test` 07.22.32).
- 8.0.8 applied the model `fixed` translation `[-7.5, -16, -8]` by hand **and** `renderBakedModel` still applies `ItemDisplayContext.FIXED`. Doubled offset.
- Renderer now lets FIXED apply once.

## World leave hang

- Save/quit stuck at `Saving worlds` (07:22:36, no `Saving chunks`). `setRemoved` / fence entity unload called `fastener.remove()`, which `dest.get`s the other end under `CONNECTION_MAP_LOCK`.
- Unload now `detachLocal()` (clear maps, no peer lookup). Intentional break still `dropItems` + `remove()` from `FastenerBlock.onRemove` / fence `KILLED`.
